[package]
name = "ferrelex_core"
version = "0.2.0"
edition = "2024"
description = "Core runtime types for ferrelex — LexBuf, character sets, and Unicode data"
keywords = ["lexer", "lexical-analysis", "parsing"]
categories = ["parsing", "text-processing"]

[dependencies]
rustc-hash = "2.1.1"

[dev-dependencies]
proptest = "1"
